{
  "name": "backbone-beer",
  "version": "0.0.0",
  "description": "Because everything I do has to deal with beer, apparently. Sheesh.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "swashcap@gmail.com",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "gulp": "^3.6.1",
    "gulp-sass": "^0.7.1",
    "gulp-autoprefixer": "0.0.7",
    "gulp-connect": "^2.0.5"
  }
}
